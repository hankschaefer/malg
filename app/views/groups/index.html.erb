<h3 class = "mt-3"> Join Group </h3>
<hr>
<% if @groups.count != 0 %>
    <% @groups.each do |group| %> 
      <%= group.name + " | "%>
      <%= link_to "JOIN", group_path(group)%> 
      <br>
  <% end %>
<% else %>
  <p> No groups to join ... create one! </p>
<% end %>