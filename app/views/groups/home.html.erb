<h3 class = "mt-3"> Group Information </h3>
<hr>
<% if current_user.group_id != nil %>
  <h4> You're in group <b> <%= @current_group.name %> </b> </h4>
  <h5> Fellow members: </h5>
  <ul>
    <% @current_group.users.each do |user| %>
    <li> <%= user.email %> </li>
    <% end %>
  </ul>
  <div class = "d-flex">
    <%= link_to 'Leave Group', group_path(current_user.group_id), :class => "m-1 btn btn-danger"%>
  </div>
<% else %>
  <h4> You aren't in a group yet! </h4>
  <div class = "d-flex">
    <%= link_to 'Create Group', new_group_path, :class => "m-1 btn btn-success" %>
    <%= link_to 'Join Group', "/groups", :class => "m-1 btn btn-primary" %>
  </div>
<% end %>